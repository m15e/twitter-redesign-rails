<strong>@<%= current_user ? current_user.username : 'nouser' %></strong>
<%= link_to 'users', users_path %>
<%= link_to 'sources', sources_path %>

<% if current_user  %>
  <%= link_to 'logout', sessions_logout_path %>
  <p>Following</p>
  <%= current_user.followeds.count %>
  <p>Followers</p>
  <%= current_user.followers.count %>
<% else %>
 <%= link_to 'login', login_path %>
<% end %>