<h1>Users#show</h1>

<%= @user.username %>

<p>followers</p>
<strong><%= @user.followers.any? ? @user.followers.first.username : 'no followers' %></strong>

<p>followed</p>
<%= @user.followeds.first.username %>