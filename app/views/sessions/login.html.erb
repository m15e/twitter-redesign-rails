<div class="login">
  <%= form_tag(sessions_try_login_path, :method => :post) do %>
    <%= label_tag(:username) %>
    <%= text_field_tag(:username, params[:username]) %>

    <%= submit_tag('Log In') %>

  <% end %>
</div>